	<div class="row">
	  <div class="col-md-9">
	    <ul id="conversas-list">
	      <% @conversas.each do |conversa| %>
	        <%= render 'conversas/conversa', conversa: conversa, user: current_user %>
	      <% end %>
	    </ul>
	</div>

	<div class="col-md-3">
    <div class="panel panel-primary">
      <div class="panel-heading">
        <h3 class="panel-title">ConexionesS</h3>
      </div>
      <div class="panel-body">
        <ul>
          <% @users.each do |user| %>
            <li><%= user.email %></li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
</div>