<h1>Mensajes</h1>

<%= sync partial: 'mensaje_row', collection: @mensajes %>
<%= sync_new partial: 'mensaje_row', resource: Mensaje.new %>

<hr>

<%= form for @new_mensaje, remote: true do |f| %>
  <%= f.text_field :body %>
<% end %>

<!--
<p id="notice"><%= notice %></p>

<h1>Mensajes</h1>

<table>
  <thead>
    <tr>
      <th>Body</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @mensajes.each do |mensaje| %>
      <tr>
        <td><%= mensaje.body %></td>
        <td><%= mensaje.user %></td>
        <td><%= link_to 'Show', mensaje %></td>
        <td><%= link_to 'Edit', edit_mensaje_path(mensaje) %></td>
        <td><%= link_to 'Destroy', mensaje, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Mensaje', new_mensaje_path %>
-->
