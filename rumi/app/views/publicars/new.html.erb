<h2> Nueva Casa </h2>
<hr>

<%= form_for @publicar, htlml: { multipart: true } do |f| %>
	<%= f.label :image %>
	<%= f.file_field :image %>

	<br>
	%= f.label :description %>
	<%= f.text_area :description %>
	<br>
	<%= f.submit "Publicar" %>
<% end %>